import React from "react";
import Header from "../Header/Header";

const About = () => {
  return (
    <div>
      <h1>This is the real about page(From about component).</h1>
    </div>
  );
};

export default About;
